---
description: Правило консистентности MVVM архитектуры - проверка связанности Model-ViewModel-View-Service
globs: [
  "**/Models/**/*.cs",
  "**/ViewModels/**/*.cs", 
  "**/Views/**/*.cs",
  "**/Views/**/*.axaml",
  "**/Services/**/*.cs",
  "**/Infrastructure/**/*.cs"
]
alwaysApply: false
priority: 1
tags: [mvvm, architecture, consistency, model, view, viewmodel, service]
---
# ПРАВИЛО КОНСИСТЕНТНОСТИ MVVM АРХИТЕКТУРЫ

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО ПРОВЕРКИ

**При изменении любого элемента проекта (Model, ViewModel, View, Service) - ОБЯЗАТЕЛЬНО проверь и обнови связанные компоненты в архитектуре MVVM для предотвращения ошибок компиляции и runtime ошибок.**

## 🔄 ОБЯЗАТЕЛЬНАЯ ЦЕПОЧКА ПРОВЕРОК

### При изменении **Model** (Domain Entity):
```
Model изменен → Проверь и обнови:
├── 📊 ApplicationDbContext (DbSet, конфигурации)
├── 🔧 Service (интерфейс + реализация)
├── 🎯 ViewModel (свойства, команды, валидация)
├── 🎨 View (привязки данных, UI элементы)
├── 🗄️ Миграции базы данных
└── 📋 Тесты (unit, integration)
```

### При изменении **ViewModel**:
```
ViewModel изменен → Проверь и обнови:
├── 🎨 View (привязки данных, команды)
├── 🔧 Service (методы, возвращаемые типы)
├── 📊 Model (если нужны новые свойства)
├── 🧭 Navigation (роуты, параметры)
└── 📋 Тесты ViewModels
```

### При изменении **View** (AXAML):
```
View изменен → Проверь и обнови:
├── 🎯 ViewModel (команды, свойства для привязки)
├── 🎨 Стили (CSS классы, ресурсы)
├── 🧭 AppViewLocator (связка View ↔ ViewModel)
├── 🔧 Converters (если нужны новые преобразования)
└── 📱 Адаптивность (мобильная версия)
```

### При изменении **Service**:
```
Service изменен → Проверь и обнови:
├── 🎯 ViewModel (вызовы методов сервиса)
├── 📊 Model (если изменились требования к данным)
├── 🔌 DI Container (регистрация зависимостей)
├── 🧪 Mock сервисы для тестов
└── 📋 Интеграционные тесты
```

## ⚡ БЫСТРЫЙ ЧЕКЛИСТ ПРОВЕРКИ

### 🔍 **Шаг 1: Компиляция**
- [ ] Проект компилируется без ошибок
- [ ] Все using директивы актуальны
- [ ] Нет неиспользуемых импортов
- [ ] Все зависимости разрешены

### 🔗 **Шаг 2: Привязки данных**
- [ ] Все `{Binding}` в View соответствуют свойствам ViewModel
- [ ] Команды в View привязаны к `ReactiveCommand` в ViewModel
- [ ] Converters работают с новыми типами данных
- [ ] Validation правила обновлены

### 🗄️ **Шаг 3: База данных**
- [ ] DbSet в ApplicationDbContext обновлен
- [ ] Миграции созданы и применены
- [ ] Связи между сущностями корректны
- [ ] Индексы и ограничения актуальны

### 🔧 **Шаг 4: Сервисы**
- [ ] Интерфейсы сервисов обновлены
- [ ] Реализации сервисов соответствуют интерфейсам
- [ ] DI регистрация актуальна
- [ ] Валидация в сервисах обновлена

### 🧭 **Шаг 5: Навигация**
- [ ] Route атрибуты актуальны
- [ ] AppViewLocator содержит новые связки
- [ ] Параметры навигации корректны
- [ ] Права доступа к страницам обновлены

## 🛠️ ИНСТРУМЕНТЫ ПРОВЕРКИ

### Автоматические проверки:
```bash
# Компиляция проекта
dotnet build

# Проверка миграций
dotnet ef migrations list

# Запуск тестов
dotnet test

# Анализ кода
dotnet format --verify-no-changes
```

### Ручные проверки:
1. **Поиск по коду**: Найди все упоминания измененного элемента
2. **Проверка привязок**: Убедись, что все `{Binding}` работают
3. **Тестирование UI**: Проверь работу форм и команд
4. **Проверка навигации**: Убедись, что переходы работают

## 🚨 ТИПИЧНЫЕ ОШИБКИ И ИХ ПРЕДОТВРАЩЕНИЕ

### ❌ **Ошибка**: Изменил Model, забыл обновить ViewModel
```csharp
// Model изменен
public class Student 
{
    public string NewProperty { get; set; } // ДОБАВЛЕНО
}

// ViewModel НЕ ОБНОВЛЕН - ОШИБКА!
public class StudentViewModel 
{
    // Отсутствует NewProperty - привязка не работает
}
```

### ✅ **Правильно**: Синхронное обновление
```csharp
// Model
public class Student 
{
    public string NewProperty { get; set; }
}

// ViewModel ОБНОВЛЕН
public class StudentViewModel 
{
    [Reactive] public string NewProperty { get; set; }
}

// View ОБНОВЛЕН
<TextBox Text="{Binding NewProperty}" />
```

### ❌ **Ошибка**: Изменил Service, забыл обновить DI
```csharp
// Добавил новый параметр в конструктор
public class StudentService(INewDependency newDep) : IStudentService
{
    // ...
}

// DI НЕ ОБНОВЛЕН - runtime ошибка!
```

### ✅ **Правильно**: Обновление DI
```csharp
// DependencyInjectionExtensions.cs ОБНОВЛЕН
services.AddScoped<INewDependency, NewDependency>();
services.AddScoped<IStudentService, StudentService>();
```

## 📋 ПРОЦЕДУРА ИЗМЕНЕНИЙ

### 1. **Планирование изменений**
- Определи область влияния изменений
- Составь список компонентов для обновления
- Проверь зависимости между компонентами

### 2. **Выполнение изменений**
- Начни с Model (если нужно)
- Обнови Service
- Обнови ViewModel
- Обнови View
- Обнови тесты

### 3. **Проверка результата**
- Компиляция без ошибок
- Все тесты проходят
- UI работает корректно
- Навигация функционирует

### 4. **Документирование**
- Обнови соответствующую документацию
- Добавь комментарии к сложным изменениям
- Обнови changelog если нужно

## ⚠️ ПОСЛЕДСТВИЯ НАРУШЕНИЯ

**Игнорирование этого правила приводит к:**
- 💥 Ошибкам компиляции
- 🐛 Runtime ошибкам
- 🔗 Сломанным привязкам данных
- 🧭 Неработающей навигации
- 📊 Ошибкам базы данных
- 🧪 Падающим тестам
- ⏰ Потере времени на отладку

## 🎯 ПРИМЕРЫ ПРИМЕНЕНИЯ

### Пример 1: Добавление нового поля в Student
```
1. Model: Добавил свойство `MiddleName` в Student.cs
2. DbContext: Обновил конфигурацию Student
3. Migration: Создал миграцию AddMiddleNameToStudent
4. Service: Обновил валидацию в StudentService
5. ViewModel: Добавил [Reactive] MiddleName в StudentViewModel
6. View: Добавил TextBox для MiddleName в StudentView.axaml
7. Tests: Обновил тесты для нового поля
```

### Пример 2: Изменение команды в ViewModel
```
1. ViewModel: Изменил параметры SaveCommand
2. View: Обновил CommandParameter в Button
3. Service: Обновил метод SaveStudent для новых параметров
4. Tests: Обновил тесты команды
```

---

**ПОМНИ: В MVVM архитектуре все компоненты взаимосвязаны. Изменение одного элемента требует проверки и обновления связанных компонентов для поддержания целостности системы.** 